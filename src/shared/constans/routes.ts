export enum AppRoutes {
  Main = '/',
  Catalog = '/catalog',
  Product = 'product',
  Basket = '/basket'
}

export enum FetchRoutes {
  Cameras = '/cameras',
  Promo = '/promo',
  Similar = '/similar',
}
